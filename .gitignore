vendor
node_modules
.devenv
.direnv
result
wp-cloud-files.zip

# Test artefacts. `vendor` above has no leading slash, so it already covers
# tests/tools/vendor.
.phpunit.result.cache
coverage/
tests/.plugin-dir
tests/.wp
